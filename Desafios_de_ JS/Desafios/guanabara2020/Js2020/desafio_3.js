function sucessor(){
    let num = parseInt(window.prompt('Digite um número inteiro qualquer: '));
    let antecessor = (num -1)
    let sucesso = (num + 1)
    let resolucao = window.alert(`Antes de ${num} temos o número ${antecessor}\nDepois de ${num}, temos o número ${sucesso}`)
}